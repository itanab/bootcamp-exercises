DELETE
FROM `region`
WHERE `slug` LIKE '%america%'

--any string before and after america

--if we want to checkbefore delete then first do the select to see if it is working

--autoincrement is not working - deleting is not arranging the id-s

DROP TABLE IF EXISTS `users` \

--to delete the entire table